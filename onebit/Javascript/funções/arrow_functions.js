const doubleVelocity = (velocity) =>{ // seta que declara funçao anonima
    return velocity * 2
}

/*tambem pode ser feito assim:

const doubleVelocity = (velocity) => velocity * 2


*/


console.log(doubleVelocity(60))