let velocity = 50
let acceleration = 5

while(velocity <= 100 ) {
    console.log("Acelerando: estamos a " + velocity + "km/s")
    velocity += acceleration //msm q: velocity = velocity + acceleration
}
